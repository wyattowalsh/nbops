---
status: active
type: goal-prompts
change: generalize-notebook-runtime-observer
tags:
  - goal
  - prompts
  - nbops
updated: 2026-08-22
cssclasses:
  - planning-doc
---

# Copyable goal prompts

## Codex

```text
/goal Implement OpenSpec change `generalize-notebook-runtime-observer` compatibility-first. The exact canonical product/distribution/import/CLI/source/default-artifact identity is `nbops`. Read CODEX_KICKOFF_PROMPT.md, IDENTITY_MAP.md, LEGACY_IDENTITY_NOTICE.md, both OpenSpec changes, ADR-016, naming-packaging-strategy.md, compatibility-migration.md, validation.md, traceability-matrix.md, PLANS.md, and outer-loop.md. Complete TASK-103, TASK-105, and TASK-106 before runtime-profile or adapter extraction. Preserve supported Colab behavior, prior artifact readability, static fallback, privacy/security, and no-keepalive boundaries. Implement by dependency order and exact write scope, validate after each slice, update ledgers, and stop for installs, network/external runtimes, server enablement, remote repository or registry actions, publication, deployment, domains/handles, secrets, permissions, or OpenSpec lifecycle actions without approval. Done only when current surfaces converge on nbops and every support claim has executed evidence or an exact blocker.
```

Codex status: `/goal`. Lifecycle: `/goal pause`, `/goal resume`, `/goal clear`.

## Grok Build

```text
/goal Implement `generalize-notebook-runtime-observer` from the OpenSpec change pack. Use `nbops` as the canonical current identity. Start with TASK-103, TASK-105, and TASK-106; preserve Colab behavior and explicit, evidence-backed legacy compatibility; keep platform detection passive, bounded, and evidence-gated. Update PLANS and outer-loop after each slice. Stop for missing representative evidence, approval, scope drift, unsafe tooling, remote repository/registry/publication/deployment actions, or archive actions.
```

Verify current Goal controls before use. Do not install, upgrade, sign in, subscribe, or run network setup automatically.

## Generic agent

```text
Goal: Adopt nbops locally and generalize the notebook runtime observer.
Context: Read the active OpenSpec change, stable historical baseline, ADR-016, migration plan, tasks, validation, traceability, PLANS, and handoff.
Constraints: Freeze behavior first; one nbops distribution and core; old identifiers are legacy/provenance only; no unsupported support/scope claims; no installs, network, remote identity, publication, deployment, or secret actions without approval.
Done when: Tasks and executed evidence agree, current identity is nbops, supported legacy behavior remains explicit, and remaining tasks have exact blockers or are dependency-ready.
```
