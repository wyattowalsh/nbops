# Legacy identity and provenance notice

The canonical current identity is **`nbops`**.

This context pack intentionally retains the stable OpenSpec change ID `build-colab-observer` and the corresponding baseline planning directory. Those paths record the project's Google Colab-first origin and prior implementation evidence. They must not be renamed merely for branding consistency because downstream tasks, evidence, and traceability refer to them.

Any prior import-package or source-package identity found in a target repository is migration evidence, not the canonical name. Codex must freeze observable behavior before moving implementation paths and may retain a compatibility shim only when verified external usage requires one.
