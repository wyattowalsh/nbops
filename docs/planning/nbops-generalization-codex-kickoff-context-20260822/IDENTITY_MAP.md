# Canonical identity map

**Canonical product:** `nbops`  
**Canonical repository identity:** `nbops`  
**Canonical Python distribution:** `nbops`  
**Canonical Python import package:** `nbops`  
**Canonical CLI:** `nbops`  
**Canonical source package target:** `src/nbops/`  
**Tagline:** Local-first runtime observability for Python notebooks.  
**Flagship adapter:** Google Colab

## Stable identifiers

The following OpenSpec identifiers remain unchanged because they are durable historical or active change IDs, not product brands:

- `build-colab-observer`
- `generalize-notebook-runtime-observer`

The baseline paths under `docs/planning/build-colab-observer/` and `openspec/changes/build-colab-observer/` are retained for provenance and executed evidence.

## Legacy compatibility rule

A pre-`nbops` import or source path may appear only as:

1. migration input discovered from the target repository;
2. an evidence-backed temporary compatibility shim;
3. historical provenance in the preserved baseline artifacts.

It is never a competing canonical product, package, import, CLI, repository, or new-artifact identity.
